<!DOCTYPE html>
<html lang="en">
  <%- include('layouts/headerTeacher'); -%>
  <body>
    <h1><%= lecture.lecturename %></h1>
    <%- include('layouts/nav'); -%>

    <main>
      <div class="lecture-information">
        <h2><%= lecture.lecturename %></h2>
        <p>Time and date: <time><%= lecture.formattedDate %> - <%= lecture.time %></time></p>
        <p>Teacher: <%= lecture.firstname %> <%= lecture.lastname %></p>
        <p>Location: <%= lecture.location %></p>
        <p>Course: <%= lecture.title %></p>
        <p><%= lecture.comment %></p>


        <div id="removeLecture">
          <% if (user.userType === 'teacher' && user.id === lecture.teacher_id){ %>
            <button onclick="deleteLecture(<%= lecture.id %>)">
              Delete lecture
            </button>
          <% } %>
        </div>


        <div id="joinLecture">
          <% if (user.userType === 'student') { %>
            <form
                    action="/lecture"
                    method="POST"
                    enctype="application/x-www-form-urlencoded"
                    name="Join Lecture"
            >
              <input
                      type="text"
                      value="<%= lecture.id %>"
                      style="display: none;"
                      name="lectureId"
              />
              <button type="submit">
                Join lecture
              </button>
            </form>
          <% } %>
        </div>


<p>Joined students:</p>
        <ul><% for (var i = 0; i < listOfStudents.length; i++) {%> <li>
              <%= listOfStudents[i].firstname %> <%= listOfStudents[i].lastname %>
            <%}%> </li> </ul>
      </div>
    </main>



    <%- include('layouts/scripts'); -%>
  </body>
</html>
